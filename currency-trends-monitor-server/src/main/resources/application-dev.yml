spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/currency_trends_monitor_dev
    username: ctms_dev_user
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: mysql_database_create.sql